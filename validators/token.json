{
  "title": "new user",
  "description": "describes how to send the token",
  "type": "object",
  "properties": {
    "token": {
      "type": "string",
      "description": "token of the user given by firebase",
      "minLength": 1
    }
  },
  "required": ["token"]
}