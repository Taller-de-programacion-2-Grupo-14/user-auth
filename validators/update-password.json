{
  "title": "change password user",
  "description": "describes properties required to change password of a user",
  "type": "object",
  "properties": {
    "password": {
      "type": "string",
      "minLength": 8,
      "description": "old password for the account user"
    },
    "newPassword": {
      "type": "string",
      "minLength": 8,
      "description": "password of the account user"
    }
  },
  "required": ["newPassword", "password"]
}